# Инструкция по развертыванию на Netlify

Это руководство поможет вам развернуть Telegram бота Pocket Accountant на Netlify.

## Предварительные требования

1. Аккаунт на [Netlify](https://www.netlify.com/)
2. Аккаунт на [Telegram](https://telegram.org/) и созданный бот через [@BotFather](https://t.me/botfather)
3. База данных (рекомендуется PostgreSQL через [Supabase](https://supabase.com/) или [Railway](https://railway.app/))

## Шаг 1: Подготовка базы данных

📖 **Подробная инструкция:** См. [DATABASE_SETUP.md](./DATABASE_SETUP.md)

### Краткая инструкция:

1. **Создайте базу данных PostgreSQL** на одной из платформ:
   - [Supabase](https://supabase.com/) (рекомендуется, бесплатно)
   - [Railway](https://railway.app/) (бесплатно)
   - [Render](https://render.com/) (бесплатно)

2. **Получите строку подключения (DATABASE_URL)** в формате:
   ```
   postgresql://user:password@host:port/database?sslmode=require
   ```

3. **Используйте автоматический скрипт для настройки:**
   ```bash
   npm run db:setup:migrate
   ```

   Или следуйте подробной инструкции в [DATABASE_SETUP.md](./DATABASE_SETUP.md)

## Шаг 2: Настройка переменных окружения

В Netlify Dashboard:

1. Перейдите в ваш сайт → **Site settings** → **Environment variables**
2. Добавьте следующие переменные:
   - `BOT_TOKEN` - токен вашего Telegram бота (получите у @BotFather)
   - `DATABASE_URL` - строка подключения к базе данных
   - `DEFAULT_CURRENCY` (опционально) - валюта по умолчанию (по умолчанию: RUB)
   - `LOG_LEVEL` (опционально) - уровень логирования (по умолчанию: info)
   - `NODE_ENV` - установите в `production`

## Шаг 3: Развертывание на Netlify

### Вариант A: Через Git (рекомендуется)

1. Загрузите проект в GitHub/GitLab/Bitbucket
2. В Netlify Dashboard:
   - Нажмите **Add new site** → **Import an existing project**
   - Выберите ваш репозиторий
   - Настройки сборки:
     - **Build command**: `npm run build`
     - **Publish directory**: `.` (или оставьте пустым)
   - Нажмите **Deploy site**

### Вариант B: Через Netlify CLI

```bash
# Установите Netlify CLI
npm install -g netlify-cli

# Войдите в Netlify
netlify login

# Перейдите в директорию проекта
cd cursor/pocket-accountant-bot

# Инициализируйте сайт
netlify init

# Разверните
netlify deploy --prod
```

## Шаг 4: Настройка миграций базы данных

После первого развертывания нужно выполнить миграции:

1. **Локально** (рекомендуется):
   ```bash
   cd cursor/pocket-accountant-bot
   npx prisma migrate deploy
   ```

2. **Или через Netlify Build Plugin**:
   Добавьте в `netlify.toml`:
   ```toml
   [[plugins]]
     package = "@netlify/plugin-prisma"
   ```

## Шаг 5: Настройка Webhook в Telegram

После развертывания получите URL вашего сайта (например: `https://your-site.netlify.app`)

1. Установите webhook через Telegram Bot API:
   ```bash
   curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
     -H "Content-Type: application/json" \
     -d '{"url": "https://your-site.netlify.app/webhook"}'
   ```

2. Или используйте браузер:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://your-site.netlify.app/webhook
   ```

3. Проверьте, что webhook установлен:
   ```bash
   curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
   ```

## Шаг 6: Проверка работы

1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Бот должен ответить приветственным сообщением

## Устранение неполадок

### Бот не отвечает

1. Проверьте логи в Netlify Dashboard → **Functions** → **webhook** → **Logs**
2. Убедитесь, что webhook установлен правильно:
   ```bash
   curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
   ```
3. Проверьте переменные окружения в Netlify

### Ошибки базы данных

1. Убедитесь, что `DATABASE_URL` правильный
2. Проверьте, что миграции выполнены:
   ```bash
   npx prisma migrate status
   ```
3. Убедитесь, что база данных доступна из интернета

### Ошибки сборки

1. Проверьте логи сборки в Netlify Dashboard
2. Убедитесь, что все зависимости установлены
3. Проверьте, что `npm run build` выполняется успешно локально

## Отключение webhook (возврат к polling)

Если нужно вернуться к локальному запуску:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook"
```

Затем запустите локально:
```bash
npm run dev
```

## Дополнительные ресурсы

- [Документация Netlify Functions](https://docs.netlify.com/functions/overview/)
- [Документация Telegram Bot API](https://core.telegram.org/bots/api)
- [Документация Prisma](https://www.prisma.io/docs)

