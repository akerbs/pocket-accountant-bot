# Pocket Accountant Bot

Телеграм‑бот для ручного учёта личных расходов с группировкой по категориям, умными подсказками и лимитами.

## Возможности

- быстрый ввод покупок в формате `сумма; категория; комментарий`;
- автоматическое создание и эмодзи‑иконки для категорий;
- прогрессивные текстовые виджеты по дням/неделям/месяцам;
- рекомендации на основе дисбаланса трат и ближайших лимитов;
- предупреждения при достижении 90 % лимита по категории;
- кастомные лимиты с подсветкой в статистике.

## Быстрый старт

```bash
cd pocket-accountant-bot
npm install
npx prisma generate
npx prisma migrate dev --name init
cp env.example .env
```

В файле `.env` заполните `BOT_TOKEN` и (при необходимости) `DATABASE_URL`.

```bash
npm run dev
```

## Основные сценарии

- `/start` — приветствие, создание пользователя и подбор клавиатуры;
- `➕ Добавить расход` — бот попросит ввести покупку в одном сообщении;
- `📊 Статистика` — дайджест по ключевым периодам и категориям;
- `🎯 Лимиты` — просмотр и установка лимитов;
- `🧠 Советы` — персональные текстовые рекомендации.

## Архитектура

Проект следует принципу DRY (Don't Repeat Yourself) и использует чистую архитектуру с разделением ответственности:

- **`BotPlatform`** — интерфейс для абстракции платформы;
- **`BotHandler`** — общая бизнес-логика, работающая через интерфейс платформы;
- **`TelegramBotAdapter`** — реализация для Telegram через Telegraf;
- **Сервисы** (`*Service`) — инкапсулируют доменную логику;
- **Prisma + PostgreSQL** — надёжное хранение данных (для production используйте облачную БД).

### Структура проекта

```
src/
├── platforms/          # Адаптеры для платформ
│   ├── bot-platform.interface.ts  # Интерфейс платформы
│   └── telegram-bot.adapter.ts    # Реализация для Telegram
├── handlers/           # Обработчики бизнес-логики
│   └── bot-handler.ts  # Общий обработчик (DRY)
├── services/           # Доменные сервисы
└── config/             # Конфигурация
```

## Переменные окружения

| Переменная      | Назначение                                   |
| --------------- | -------------------------------------------- |
| `BOT_TOKEN`     | токен Telegram Bot API                       |
| `DATABASE_URL`  | строка подключения PostgreSQL (см. [DATABASE_SETUP.md](./DATABASE_SETUP.md)) |
| `DEFAULT_CURRENCY` | валюта по умолчанию (по умолч. `RUB`)    |

## Разработка

### Принципы архитектуры

- **DRY (Don't Repeat Yourself):** Вся бизнес-логика находится в `BotHandler`
- **SOLID:** Разделение ответственности через интерфейсы и адаптеры
- **KISS:** Простая и понятная структура кода

## Лицензия

MIT.
